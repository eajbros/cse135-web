RewriteEngine On

# Only rewrite requests that start with /api/
RewriteRule ^api(/.*)?$ index.php [QSA,L]

# (optional) If you want /api to also work without trailing slash:
RewriteRule ^api$ index.php [QSA,L]